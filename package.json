{
  "name": "stan-chrome-extensions-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Synthetic-log test harness for the Chrome extensions in this repo (parsing logic only). Lives at repo root so it is never packaged into any extension ZIP.",
  "type": "commonjs",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "jpeg-js": "^0.4.4",
    "jsdom": "^25.0.1"
  }
}
